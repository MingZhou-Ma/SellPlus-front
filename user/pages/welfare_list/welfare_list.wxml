<view class="marg-t16 tx-14 tx-999 tx-c lh-1 bg-fff bord-b1 bord-c flex1 flex-r">
    <text catchtap="widgetsToggle1" class="box padd-a16 w50e {{item.open?'bord-b3':''}}" id="{{item.id}}" style="{{item.open?'color:'+mainColor+' !important;':''}}; {{item.open?'border-color:'+mainColor:''}};" wx:for="{{xiangqing}}">{{item.text}}</text>
</view>

<view class="coupon-list" wx:if="{{list&&list.length>0}}">
    <block wx:for="{{list}}" wx:for-item="coupon" wx:if="{{coupon.status==0}}">
        <view class="coupon-item coupon-status-{{coupon.status}} {{currentTab==0?'':'hide'}}" style="margin-top:20rpx;">
            <image class="coupon-bg" src="../../resource/images/img-coupon-bg-{{coupon.status==0?0:1}}.png"></image>
            <image class="coupon-status-icon" src="../../resource/images/img-coupon-status-icon-{{coupon.status}}.png" wx:if="{{coupon.status!=0}}"></image>
            <view class="flex-row" style="height: 100%;overflow: hidden">
                <view class="flex-grow-0 flex-col flex-y-center flex-x-center coupon-left">
                    <view class="flex-row flex-y-bottom">
                        <view style="font-size: 15pt;line-height: .9">
                            ￥{{coupon.sub_price}}
                        </view>
                    </view>
                </view>
                <view class="flex-grow-1 flex-y-center coupon-right">
                    <view style="width:100%;">
                        <view class="flex-row flex-y-center mb-10" style="margin-bottom:5rpx;">
                            <view class="flex-grow-1" style="font-size: 12pt;">{{coupon.event_desc}}</view>
                        </view>
                        <!-- <view style="font-size: 8pt;color: #666666">{{coupon.begin_time}} ~ {{coupon.end_time}}</view> -->
                        <!-- <text class="user_coupon_font" >全场通用现金卷</text> -->
                        <!-- <text class="user_coupon_font" wx:if="{{coupon.appoint_type==2&&coupon.goods_id_list==null}}">全场通用</text> -->
                        <!-- <block wx:if="{{coupon.appoint_type==1&&coupon.cat.length>0}}">
                            <view class="user_coupon_font" style="overflow: hidden; white-space: nowrap; text-overflow: ellipsis;width:87%;">
                            仅限<text wx:for="{{coupon.cat}}" wx:for-item="cat" wx:key="id">{{cat.name}}、</text>产品使用
                            </view>
                            <block wx:if="{{coupon.cat.length>2}}">
                                <image bindtap="xia" class="xia" data-index="{{index}}" src="../../resource/images/user-coupon-xia.png" wx:if="{{index!=check}}"></image>
                                <image bindtap="shou" class="xia" data-index="{{index}}" src="../../resource/images/shou.png" wx:if="{{index==check}}"></image>
                            </block>
                        </block> -->
                        <!-- <view bindtap="goodsList" class="user_coupon_font" data-goods_id="{{coupon.goods_id_list}}" style="height:50rpx;" wx:if="{{coupon.appoint_type==2&&coupon.goods_id_list!=null}}">指定商品使用 点进去查看指定商品</view> -->
                        
                        <!-- <button class="open_code" wx:if="{{coupon.status==0}}" style="opacity: 0.5;color: white; text-align:center; background-color: #ff8800;"   class="marginview"  bindtap="Voucher">刮开券码</button> -->
                        <view class="coupon_code" >券码:{{coupon.code}}</view>
                    </view>
                </view>
            </view>
        </view>
        <!-- <block wx:if="{{coupon.cat.length>2}}">
            <view class="user_coupon" style="padding:10rpx 24rpx;background:#fff;" wx:if="{{check==index}}">
                    仅限<text wx:for="{{coupon.cat}}" wx:for-item="cat" wx:key="id">{{cat.name}}、</text>产品使用
                </view>
        </block> -->
    </block>
    <block wx:for="{{list}}" wx:for-item="coupon" wx:if="{{coupon.status==1}}">
        <view class="coupon-item coupon-status-{{coupon.status}} {{currentTab==1?'':'hide'}}" style="margin-top:20rpx;">
            <image class="coupon-bg" src="../../resource/images/img-coupon-bg-{{coupon.status==0?0:1}}.png"></image>
            <image class="coupon-status-icon" src="../../resource/images/img-coupon-status-icon-{{coupon.status}}.png" wx:if="{{coupon.status!=0}}"></image>
            <view class="flex-row" style="height: 100%;overflow: hidden">
                <view class="flex-grow-0 flex-col flex-y-center flex-x-center coupon-left">
                    <view class="flex-row flex-y-bottom">
                        <view style="font-size: 15pt;line-height: .9">
                            ￥{{coupon.sub_price}}
                        </view>
                    </view>
                </view>
                <view class="flex-grow-1 flex-y-center coupon-right">
                    <view style="width:100%;">
                        <view class="flex-row flex-y-center mb-10" style="margin-bottom:5rpx;">
                            <view class="flex-grow-1" style="font-size: 12pt;">{{coupon.event_desc}}</view>
                        </view>
                        <!-- <view style="font-size: 8pt;color: #666666">{{coupon.begin_time}} ~ {{coupon.end_time}}</view> -->
                        <!-- <text class="user_coupon_font" >全场通用现金卷</text> -->
                        <!-- <text class="user_coupon_font" wx:if="{{coupon.appoint_type==2&&coupon.goods_id_list==null}}">全场通用</text> -->
                        <!-- <block wx:if="{{coupon.appoint_type==1&&coupon.cat.length>0}}">
                            <view class="user_coupon_font" style="overflow: hidden; white-space: nowrap; text-overflow: ellipsis;width:87%;">
                            仅限<text wx:for="{{coupon.cat}}" wx:for-item="cat" wx:key="id">{{cat.name}}、</text>产品使用
                            </view>
                            <block wx:if="{{coupon.cat.length>2}}">
                                <image bindtap="xia" class="xia" data-index="{{index}}" src="../../resource/images/user-coupon-xia.png" wx:if="{{index!=check}}"></image>
                                <image bindtap="shou" class="xia" data-index="{{index}}" src="../../resource/images/shou.png" wx:if="{{index==check}}"></image>
                            </block>
                        </block> -->
                        <!-- <view bindtap="goodsList" class="user_coupon_font" data-goods_id="{{coupon.goods_id_list}}" style="height:50rpx;" wx:if="{{coupon.appoint_type==2&&coupon.goods_id_list!=null}}">指定商品使用 点进去查看指定商品</view> -->
                        <button wx:if="{{coupon.status==0}}" style="opacity: 0.5;color: white; text-align:center; background-color: #ff8800;"   class="marginview"  bindtap="Voucher">刮开券码</button>
                    </view>
                </view>
            </view>
        </view>
        <!-- <block wx:if="{{coupon.cat.length>2}}">
            <view class="user_coupon" style="padding:10rpx 24rpx;background:#fff;" wx:if="{{check==index}}">
                    仅限<text wx:for="{{coupon.cat}}" wx:for-item="cat" wx:key="id">{{cat.name}}、</text>产品使用
                </view>
        </block> -->
    </block>
</view> 
<view style="padding-top: 200rpx;color: #888;text-align: center" wx:else>暂无相关优惠券</view>
